// src/app/pages/inscripciones/inscripciones.routes.ts
import { Routes } from '@angular/router';
import { Inscripciones } from './inscripciones';

const routes: Routes = [
  {
    path: '',
    component: Inscripciones
  }
];

export default routes;
